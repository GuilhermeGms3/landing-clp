services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: hub-homepage
    restart: unless-stopped
    ports:
      - "3005:3000"
    volumes:
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock:ro
